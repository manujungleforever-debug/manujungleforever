
# Reglas y Directivas del Proyecto

- **MÓDULOS CONGELADOS Y FINALIZADOS**: NO modificar, tocar ni alterar bajo ninguna circunstancia los siguientes módulos que ya están 100% terminados y funcionales, a menos que el usuario lo solicite expresamente:
  1. **Tours** (`gestionar-tours.html`, rutas `/api/tours`)
  2. **Salidas / Departures** (`gestionar-salidas.html`, manifiesto PDF, rutas `/api/salidas`)
  3. **Testimonios** (`gestionar-testimonios.html`, rutas `/api/testimonios`)
  4. **Libro de Reclamaciones** (`gestionar-reclamos.html`, rutas `/api/reclamos`)
  5. **Medios / Media R2** (`gestionar-medios.html`, rutas `/api/media`)
  6. **Usuarios** (`gestionar-usuarios.html`, rutas `/api/users`)

- **FOOTER**: NUNCA modificar el footer del sitio web sin autorización explícita del usuario.

