{
  "name": "manujungleforever",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev www.manujungleforever.com --d1=DB=c0765748-5818-44ee-ab40-c43607a106d6 --r2=MEDIA_BUCKET=mjf-media --persist-to=.wrangler/state/v3",
    "deploy": "wrangler pages deploy www.manujungleforever.com --project-name=manujungleforever --branch=main",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:local": "wrangler d1 execute manujungleforever-db --local --file=drizzle/migrations/0000_nebulous_deathbird.sql && wrangler d1 execute manujungleforever-db --local --file=drizzle/migrations/0001_abnormal_living_lightning.sql && wrangler d1 execute manujungleforever-db --local --file=drizzle/migrations/seed_data.sql",
    "db:migrate:remote": "wrangler d1 execute manujungleforever-db --remote --file=drizzle/migrations/0000_nebulous_deathbird.sql && wrangler d1 execute manujungleforever-db --remote --file=drizzle/migrations/0001_abnormal_living_lightning.sql && wrangler d1 execute manujungleforever-db --remote --file=drizzle/migrations/seed_data.sql",
    "db:seed:local": "wrangler d1 execute manujungleforever-db --local --file=drizzle/migrations/seed_data.sql",
    "db:seed:remote": "wrangler d1 execute manujungleforever-db --remote --file=drizzle/migrations/seed_data.sql",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "drizzle-orm": "^0.39.3",
    "hono": "^4.7.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "drizzle-kit": "^0.30.4",
    "playwright": "^1.62.1",
    "typescript": "^5.7.3",
    "wrangler": "^3.111.0"
  }
}
